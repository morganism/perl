#!/bin/bash
#for i in `ls -1 ~/datafeed/um/pid/*.pid`; do  BOX=`cat $i | awk -F: '{print $1}'`; echo $BOX $i; done
for i in `ls -1 ~/datafeed/um/pid/*.pid`; do  BOX=`cat $i | awk -F: '{print $1}'`; LS=`ls -l $i` ; echo $BOX $LS; done

